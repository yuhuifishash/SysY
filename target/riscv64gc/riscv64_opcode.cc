#include "riscv64_opcode.h"
struct RvOpInfo OpTable[] = {
	RiscVOP(R,SLL),
	RiscVOP(I,SLLI),
	RiscVOP(R,SRL),
	RiscVOP(I,SRLI),
	RiscVOP(R,SRA),
	RiscVOP(I,SRAI),
	RiscVOP(R,ADD),
	RiscVOP(I,ADDI),
	RiscVOP(R,SUB),
	RiscVOP(U,LUI),
	RiscVOP(U,AUIPC),
	RiscVOP(R,XOR),
	RiscVOP(I,XORI),
	RiscVOP(R,OR),
	RiscVOP(I,ORI),
	RiscVOP(R,AND),
	RiscVOP(I,ANDI),
	RiscVOP(R,SLT),
	RiscVOP(I,SLTI),
	RiscVOP(R,SLTU),
	RiscVOP(I,SLTIU),
	RiscVOP(B,BEQ),
	RiscVOP(B,BNE),
	RiscVOP(B,BLT),
	RiscVOP(B,BGE),
	RiscVOP(B,BLTU),
	RiscVOP(B,BGEU),
	RiscVOP(J,JAL),
	RiscVOP(I,JALR),
	RiscVOP(I,LB),
	RiscVOP(I,LH),
	RiscVOP(I,LBU),
	RiscVOP(I,LHU),
	RiscVOP(I,LW),
	RiscVOP(S,SB),
	RiscVOP(S,SH),
	RiscVOP(S,SW),
	RiscVOP(R,SLLW),
	RiscVOP(I,SLLIW),
	RiscVOP(R,SRLW),
	RiscVOP(I,SRLIW),
	RiscVOP(R,SRAW),
	RiscVOP(I,SRAIW),
	RiscVOP(R,ADDW),
	RiscVOP(I,ADDIW),
	RiscVOP(R,SUBW),
	RiscVOP(I,LWU),
	RiscVOP(I,LD),
	RiscVOP(S,SD),

	RiscVOP(R,MUL),
	RiscVOP(R,MULH),
	RiscVOP(R,MULHSU),
	RiscVOP(R,MULHU),
	RiscVOP(R,DIV),
	RiscVOP(R,DIVU),
	RiscVOP(R,REM),
	RiscVOP(R,REMU),
	RiscVOP(R,MULW),
	RiscVOP(R,DIVW),
	RiscVOP(R,REMW),
	RiscVOP(R,REMUW),
};